{
  "name": "decidim-extra_censuses",
  "version": "0.1.0",
  "description": "Extra Censuses For the Election Component",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "lint": "eslint -c .eslintrc.json --no-error-on-unmatched-pattern --ignore-pattern app/packs/vendor --ext .js app/packs",
    "stylelint": "stylelint app/packs/**/*.scss"
  },
  "engines": {
    "node": "22.14.0",
    "npm": ">=9.6.7"
  },
  "devDependencies": {
    "@decidim/eslint-config": "0.32.1",
    "@decidim/prettier-config": "0.32.1",
    "@decidim/stylelint-config": "0.32.1",
    "postcss-scss": "^4.0.9"
  }
}
